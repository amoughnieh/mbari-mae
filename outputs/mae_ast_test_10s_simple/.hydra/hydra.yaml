hydra:
  run:
    dir: /home/incantator/Documents/mbari-mae/outputs/mae_ast_test_10s_simple
  sweep:
    dir: ???
    subdir: ${hydra.job.config_name}__${hydra.job.override_dirname}
  hydra_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][HYDRA] %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers:
      - console
    loggers:
      logging_example:
        level: DEBUG
    disable_existing_loggers: false
  job_logging:
    version: 1
    formatters:
      simple:
        format: '[%(asctime)s][%(name)s][%(levelname)s] - %(message)s'
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
      file:
        class: logging.FileHandler
        formatter: simple
        filename: ${hydra.job.name}.log
    root:
      level: INFO
      handlers:
      - console
      - file
    disable_existing_loggers: false
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    hydra_help: ???
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.run.dir=/home/incantator/Documents/mbari-mae/outputs/mae_ast_test_10s_simple
    task:
    - task.data=/home/incantator/Documents/mbari-mae/data/manifests/audio_chunks-MARS-20171030T000000Z-10secs
    - common.user_dir=/home/incantator/Documents/mbari-mae/mae_ast
    - model._name=mae_ast
    - criterion._name=mae_ast
    - dataset.max_tokens=25000
    - task.max_sample_size=20000
    - +task.max_keep_size=20000
    - task.sample_rate=2000
    - optimization.max_update=100
    - common.log_interval=10
    - distributed_training.distributed_world_size=1
    - distributed_training.nprocs_per_node=1
  job:
    name: hydra_train
    override_dirname: +task.max_keep_size-20000__common.log_interval-10__common.user_dir-/home/incantator/Documents/mbari-mae/mae_ast__criterion._name-mae_ast__dataset.max_tokens-25000__distributed_training.distributed_world_size-1__distributed_training.nprocs_per_node-1__model._name-mae_ast__optimization.max_update-100__task.max_sample_size-20000__task.sample_rate-2000
    id: ???
    num: ???
    config_name: mbari_10s_2khz.yaml
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '-'
        item_sep: __
        exclude_keys:
        - run
        - task.data
  runtime:
    version: 1.0.7
    cwd: /home/incantator/Documents/mbari-mae
  verbose: false
